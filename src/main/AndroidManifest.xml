<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tp2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
	android:name=".AppTest"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Emploi du temps"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EvaluationDetail" />
        <activity android:name=".NextCour" />
        <activity android:name=".CourAujourdhui" />
        <activity android:name=".ChoisirJour" />
        <activity android:name=".option" />
        <activity android:name=".LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>

</manifest>